<%-
  args = [ "--partition", "quick", "--nodes", "1", "--ntasks-per-node", "1" ]
-%>
---
batch_connect:
  template: vnc
script:
  accounting_id: "<%= account %>"
  native:
    <%- args.each do |arg| -%>
    - "<%= arg %>"
    <%- end -%>
